import styles from "../css/Title.module.css";

const Title = () => {
  return (
    <div className={styles.main}>
      <h1 className={styles.title}>50 Movies</h1>
    </div>
  );
};

export default Title;
